[pytest]
addopts = -q --alluredir=reports/allure-results

markers =
    csv: data-driven tests using CSV rows
    smoke: quick confidence checks
    regression: broader functional coverage

log_cli = true
log_cli_level = INFO

pythonpath = src