```yaml
name: Selenium Pytest Tests

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

  workflow_dispatch:

jobs:
  selenium-tests:
    name: Chrome Headed Selenium Tests
    runs-on: windows-latest

    steps:

      # ============================================================
      # 01 - CHECKOUT
      # ============================================================
      - name: "01 - Checkout repository"
        uses: actions/checkout@v4

      # ============================================================
      # 02 - PYTHON
      # ============================================================
      - name: "02 - Setup Python"
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      # ============================================================
      # 03 - INSTALL GOOGLE CHROME
      # ============================================================
      - name: "03 - Install Google Chrome"
        shell: powershell
        run: |
          Write-Host ""
          Write-Host "============================================================"
          Write-Host " INSTALLING GOOGLE CHROME"
          Write-Host "============================================================"
          Write-Host ""

          choco install googlechrome -y --no-progress

          if ($LASTEXITCODE -ne 0) {
            Write-Host "ERROR: Google Chrome installation failed."
            exit $LASTEXITCODE
          }

          Write-Host ""
          Write-Host "Chrome installation completed."

      # ============================================================
      # 04 - FIND CHROME
      # ============================================================
      - name: "04 - Verify Chrome"
        shell: powershell
        run: |
          Write-Host ""
          Write-Host "============================================================"
          Write-Host " VERIFYING GOOGLE CHROME"
          Write-Host "============================================================"
          Write-Host ""

          $chromePaths = @(
            "${env:ProgramFiles}\Google\Chrome\Application\chrome.exe",
            "${env:ProgramFiles(x86)}\Google\Chrome\Application\chrome.exe",
            "${env:LOCALAPPDATA}\Google\Chrome\Application\chrome.exe"
          )

          $chromeFound = $false

          foreach ($path in $chromePaths) {
            if (Test-Path $path) {

              Write-Host "Chrome found:"
              Write-Host "  $path"

              $version = (Get-Item $path).VersionInfo.ProductVersion

              Write-Host ""
              Write-Host "Chrome version:"
              Write-Host "  $version"

              # Add Chrome directory to PATH for this job
              $chromeDir = Split-Path $path
              $env:PATH = "$chromeDir;$env:PATH"

              # Persist PATH for subsequent GitHub Actions steps
              "$chromeDir" | Out-File `
                -FilePath $env:GITHUB_PATH `
                -Encoding utf8 `
                -Append

              $chromeFound = $true
              break
            }
          }

          if (-not $chromeFound) {
            Write-Host ""
            Write-Host "ERROR: Chrome executable was not found."
            exit 1
          }

          Write-Host ""
          Write-Host "Chrome is ready for Selenium."

      # ============================================================
      # 05 - ENVIRONMENT INFORMATION
      # ============================================================
      - name: "05 - Verify CI environment"
        shell: powershell
        run: |
          Write-Host ""
          Write-Host "============================================================"
          Write-Host " CI ENVIRONMENT"
          Write-Host "============================================================"
          Write-Host ""

          Write-Host "Operating System:"
          Get-CimInstance Win32_OperatingSystem |
            Select-Object Caption, Version |
            Format-List

          Write-Host "Python:"
          python --version

          Write-Host "Pip:"
          python -m pip --version

          Write-Host "Chrome:"
          chrome.exe --version

          Write-Host ""
          Write-Host "Working directory:"
          Get-Location

      # ============================================================
      # 06 - UPGRADE PIP
      # ============================================================
      - name: "06 - Upgrade pip"
        shell: powershell
        run: |
          Write-Host ""
          Write-Host "============================================================"
          Write-Host " UPGRADING PIP"
          Write-Host "============================================================"
          Write-Host ""

          python -m pip install `
            --upgrade pip `
            --disable-pip-version-check

      # ============================================================
      # 07 - INSTALL PYTHON REQUIREMENTS
      # ============================================================
      - name: "07 - Install Python requirements"
        shell: powershell
        run: |
          Write-Host ""
          Write-Host "============================================================"
          Write-Host " INSTALLING PYTHON REQUIREMENTS"
          Write-Host "============================================================"
          Write-Host ""

          if (-not (Test-Path "requirements.txt")) {
            Write-Host "ERROR: requirements.txt was not found."
            exit 1
          }

          Write-Host "Installing requirements.txt..."
          Write-Host ""

          python -m pip install `
            -r requirements.txt `
            --disable-pip-version-check `
            --no-warn-script-location

          if ($LASTEXITCODE -ne 0) {
            Write-Host ""
            Write-Host "ERROR: Python dependency installation failed."
            exit $LASTEXITCODE
          }

          Write-Host ""
          Write-Host "Python dependencies installed successfully."

      # ============================================================
      # 08 - VERIFY TEST STACK
      # ============================================================
      - name: "08 - Verify test stack"
        shell: powershell
        run: |
          Write-Host ""
          Write-Host "============================================================"
          Write-Host " TEST STACK"
          Write-Host "============================================================"
          Write-Host ""

          python -c "import pytest; print('pytest        :', pytest.__version__)"
          python -c "import selenium; print('selenium       :', selenium.__version__)"
          python -c "import allure; print('allure-pytest  : installed')"
          python -c "import yaml; print('PyYAML         :', yaml.__version__)"

          Write-Host ""
          Write-Host "Python test stack verified."

      # ============================================================
      # 09 - INSTALL ALLURE
      # ============================================================
      - name: "09 - Install Allure CLI"
        shell: powershell
        run: |
          Write-Host ""
          Write-Host "============================================================"
          Write-Host " INSTALLING ALLURE CLI"
          Write-Host "============================================================"
          Write-Host ""

          choco install allure -y --no-progress

          if ($LASTEXITCODE -ne 0) {
            Write-Host "ERROR: Allure installation failed."
            exit $LASTEXITCODE
          }

          Write-Host ""
          Write-Host "Allure version:"
          allure --version

      # ============================================================
      # 10 - PREPARE DIRECTORIES
      # ============================================================
      - name: "10 - Prepare test directories"
        shell: powershell
        run: |
          Write-Host ""
          Write-Host "============================================================"
          Write-Host " PREPARING TEST DIRECTORIES"
          Write-Host "============================================================"
          Write-Host ""

          New-Item `
            -ItemType Directory `
            -Force `
            -Path "reports/allure-results" | Out-Null

          New-Item `
            -ItemType Directory `
            -Force `
            -Path "reports/allure-report" | Out-Null

          New-Item `
            -ItemType Directory `
            -Force `
            -Path "artifacts" | Out-Null

          Write-Host "reports/allure-results : READY"
          Write-Host "reports/allure-report  : READY"
          Write-Host "artifacts              : READY"

      # ============================================================
      # 11 - RUN SELENIUM TESTS - HEADED
      # ============================================================
      - name: "11 - Run Chrome headed tests"
        shell: powershell
        env:
          CI: "true"
          TEST_ENV: "test"
          PYTHONUNBUFFERED: "1"
        run: |
          Write-Host ""
          Write-Host "============================================================"
          Write-Host " RUNNING SELENIUM TESTS"
          Write-Host "============================================================"
          Write-Host ""
          Write-Host "Framework     : Selenium + Pytest"
          Write-Host "Browser       : Google Chrome"
          Write-Host "Mode          : HEADED"
          Write-Host "Environment   : TEST"
          Write-Host "Test path     : src/tests"
          Write-Host "Allure output : reports/allure-results"
          Write-Host ""
          Write-Host "ChromeDriver  : Selenium Manager"
          Write-Host ""
          Write-Host "------------------------------------------------------------"
          Write-Host " PYTEST OUTPUT"
          Write-Host "------------------------------------------------------------"
          Write-Host ""

          python -m pytest src/tests `
            --browser=chrome `
            --env=test `
            --alluredir=reports/allure-results `
            --clean-alluredir `
            -ra

          $testExitCode = $LASTEXITCODE

          Write-Host ""
          Write-Host "------------------------------------------------------------"

          if ($testExitCode -eq 0) {
            Write-Host " TEST RESULT: PASSED"
          }
          else {
            Write-Host " TEST RESULT: FAILED"
            Write-Host " Pytest exit code: $testExitCode"
          }

          Write-Host "------------------------------------------------------------"
          Write-Host ""

          exit $testExitCode

      # ============================================================
      # 12 - GENERATE ALLURE REPORT
      # ============================================================
      - name: "12 - Generate Allure HTML report"
        if: always()
        shell: powershell
        run: |
          Write-Host ""
          Write-Host "============================================================"
          Write-Host " GENERATING ALLURE REPORT"
          Write-Host "============================================================"
          Write-Host ""

          if (-not (Test-Path "reports/allure-results")) {
            Write-Host "WARNING: Allure results directory does not exist."
            exit 0
          }

          $resultFiles = Get-ChildItem `
            "reports/allure-results" `
            -File `
            -ErrorAction SilentlyContinue

          Write-Host "Allure result files: $($resultFiles.Count)"
          Write-Host ""

          if ($resultFiles.Count -eq 0) {
            Write-Host "WARNING: No Allure result files were generated."
            exit 0
          }

          Write-Host "Generating HTML report..."
          Write-Host ""

          allure generate `
            reports/allure-results `
            --clean `
            -o reports/allure-report

          if ($LASTEXITCODE -ne 0) {
            Write-Host "WARNING: Allure report generation failed."
            exit 0
          }

          Write-Host ""
          Write-Host "Allure report generated successfully."
          Write-Host "Location: reports/allure-report/index.html"

      # ============================================================
      # 13 - INSPECT GENERATED FILES
      # ============================================================
      - name: "13 - Inspect generated files"
        if: always()
        shell: powershell
        run: |
          Write-Host ""
          Write-Host "============================================================"
          Write-Host " GENERATED FILES"
          Write-Host "============================================================"
          Write-Host ""

          Write-Host "Allure results:"
          if (Test-Path "reports/allure-results") {
            $count = (Get-ChildItem "reports/allure-results" -File).Count
            Write-Host "  Files: $count"
          }
          else {
            Write-Host "  Directory not found"
          }

          Write-Host ""
          Write-Host "Allure HTML report:"
          if (Test-Path "reports/allure-report") {
            $count = (Get-ChildItem "reports/allure-report" -Recurse -File).Count
            Write-Host "  Files: $count"

            if (Test-Path "reports/allure-report/index.html") {
              Write-Host "  index.html: FOUND"
            }
          }
          else {
            Write-Host "  Directory not found"
          }

          Write-Host ""
          Write-Host "Test artifacts:"
          if (Test-Path "artifacts") {
            $count = (Get-ChildItem "artifacts" -Recurse -File).Count
            Write-Host "  Files: $count"
          }
          else {
            Write-Host "  Directory not found"
          }

      # ============================================================
      # 14 - UPLOAD ALLURE REPORT
      # ============================================================
      - name: "14 - Upload Allure HTML report"
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: reports/allure-report
          if-no-files-found: warn
          retention-days: 14

      # ============================================================
      # 15 - UPLOAD ALLURE RESULTS
      # ============================================================
      - name: "15 - Upload Allure results"
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-results
          path: reports/allure-results
          if-no-files-found: warn
          retention-days: 14

      # ============================================================
      # 16 - UPLOAD TEST ARTIFACTS
      # ============================================================
      - name: "16 - Upload test artifacts"
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-artifacts
          path: artifacts
          if-no-files-found: warn
          retention-days: 14

      # ============================================================
      # 17 - FINAL SUMMARY
      # ============================================================
      - name: "17 - CI summary"
        if: always()
        shell: powershell
        run: |
          Write-Host ""
          Write-Host "============================================================"
          Write-Host " CI TEST RUN COMPLETE"
          Write-Host "============================================================"
          Write-Host ""
          Write-Host "Framework   : Selenium + Pytest"
          Write-Host "Browser     : Google Chrome"
          Write-Host "Mode        : HEADED"
          Write-Host "Environment : TEST"
          Write-Host ""
          Write-Host "Artifacts:"
          Write-Host "  - allure-report"
          Write-Host "  - allure-results"
          Write-Host "  - test-artifacts"
          Write-Host ""
          Write-Host "Allure:"
          Write-Host "  reports/allure-report/index.html"
          Write-Host ""
          Write-Host "============================================================"
```
