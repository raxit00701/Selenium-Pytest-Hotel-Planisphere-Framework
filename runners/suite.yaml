browsers: [chrome, firefox, edge]  # one session each
markers: ""                        # e.g. "smoke"
workers: 1                         # per browser (keep 1 for cleaner session control)
extra_pytest_args: ""              # e.g., -k "login or search"
tests_path: "src/tests"
